#include<iostream.h>
#include<conio.h>
student ();
{
int mks;
cout<<"enter marks";
cin>>mks;
return (mks);
}
void marks ()
{
int m;
if (100>=m && m>75)
cout<<"A grade";
else if (75>=m && 50<m)
cout<<"B grade";
else (m>0 && m<=50)
cout<<"fail";
}
void main()
{
student s[3];
for (int i=0; i<3; i++)
{s[i].marks ;
}
getch();
}